apiVersion: apps/v1
kind: Deployment

metadata:
  name: spam-api

spec:
  replicas: 2

  selector:
    matchLabels:
      app: spam-api

  template:
    metadata:
      labels:
        app: spam-api

    spec:
      containers:
        - name: spam-api
          image: spam-api-multistage:latest
          imagePullPolicy: Never

          ports:
            - containerPort: 8000

          resources:
            requests:
              cpu: "500m"
              memory: "512Mi"

            limits:
              cpu: "500m"
              memory: "512Mi"

          readinessProbe:
            httpGet:
              path: /healthz
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 2
            failureThreshold: 3
